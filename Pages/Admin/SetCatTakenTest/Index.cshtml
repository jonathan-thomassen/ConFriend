@page
@model ConFriend.Pages.SetCatTakenTest.IndexModel
@{
    ViewData["Title"] = "SeatCategory List";
}
<div>
    <form method="post">

        <div class="form-group">
            <label asp-for="@Model.SeatType.SeatsTaken" class="control-label"></label>
            <input asp-for="@Model.SeatType.SeatsTaken" class="form-control" />
            <span asp-validation-for="SeatType.SeatsTaken" class="text-danger"></span>
        </div>
        <div class="form-group">
            <button type="submit" asp-page-handler="Save" class="btn btn btn-primary">Save New</button>
        </div>
    </form>
</div>

<h4 style="color:aquamarine"> Themes</h4>
<h1>Create Location</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                Hotel No
            </th>
            <th>
                Name
            </th>
            <th>
                Address
            </th>

        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model.SeatCategorylist)
        {
            <tr>
                <td>
                    <form method="post">

                        <div class="form-group">
                            <label asp-for="@Model.SeatType.SeatsTaken" class="control-label"></label>
                            <input asp-for="@Model.SeatType.SeatsTaken" value="@item.SeatsTaken" class="form-control" />
                            <input type="hidden" name="id2" asp-for="@item.EventId" />
                            <span asp-validation-for="SeatType.SeatsTaken" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <button type="submit" asp-page-handler="Save" name="id" value="@item.SeatCategoryId" class="btn btn btn-primary">Update</button>
                            <button type="submit" asp-page-handler="Delete" name="id" value="@item.SeatCategoryId" class="btn btn btn-warning">Delete</button>
                        </div>
                    </form>
                </td>
            </tr>

        }
</table>